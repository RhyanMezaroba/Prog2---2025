Options +FollowSymLinks
RewriteEngine On
RewriteBase /DoaSys/

# Se arquivo ou diretório real existir, serve normalmente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Encaminha tudo para index.php (front controller)
RewriteRule ^ index.php [L,QSA]
